Description

Reads student.csv.

Converts CSV data to JSON (students.json).

Allows searching students by email.

Counts students whose first name starts with a specific letter.

Files Used

Program.cs → C# code for CSV → JSON conversion.

student.csv → Input CSV file.

students.json → Output JSON file.

How to Run

Make sure student.csv is in the project folder.

Run Program2.cs.

Check output JSON and perform search/count operations via console.